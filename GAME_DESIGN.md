# 퍼펙트 링 토스 (Perfect Ring Toss) 게임 기획서

## 1. 게임 개요

| 항목 | 내용 |
|------|------|
| 게임명 | Perfect Ring Toss |
| 장르 | 캐주얼 / 아케이드 |
| 플랫폼 | 웹 브라우저 (HTML5 Canvas) |
| 목표 | 링을 바(막대)에 최대한 많이 걸기 |

## 2. 핵심 게임플레이

### 2.1 기본 규칙
- 화면에 수직으로 서있는 **바(막대)**가 있음
- 플레이어는 **링**을 던져서 바에 걸어야 함
- **성공**: 링이 바에 걸림 → 점수 획득 → 다음 링 던지기
- **실패**: 링이 바에 걸리지 않음 → **즉시 게임 오버**
- 난이도 시스템 없음 (단일 난이도)

### 2.2 게임 목표
- 실수 없이 최대한 많은 링을 연속으로 성공시키기
- 최고 기록(하이스코어) 갱신

## 3. 게임 요소

### 3.1 바 (Bar/Pole)
```
     |
     |
     |  ← 바 (링을 걸 목표물)
     |
   ━━┻━━
```
- 화면 우측에 고정 위치
- 수직으로 서있는 막대 형태
- 링이 걸릴 수 있는 히트박스 보유

### 3.2 링 (Ring)
```
   ╭───╮
   │   │  ← 링 (도넛 형태)
   ╰───╯
```
- 화면 좌측에서 던짐
- 포물선 궤적으로 날아감
- 바에 정확히 걸려야 성공

### 3.3 던지기 메커니즘
- **조준**: 마우스/터치로 각도 조절
- **파워**: 드래그 거리 또는 게이지로 힘 조절
- **발사**: 마우스 버튼 릴리즈 / 터치 릴리즈

## 4. 게임 흐름

```
[게임 시작]
     ↓
[링 조준 & 던지기]
     ↓
   성공? ──Yes──→ [점수 +1] → [다음 링 던지기]
     │
    No
     ↓
[게임 오버]
     ↓
[결과 화면: 점수 표시]
     ↓
[재시작 또는 종료]
```

## 5. UI 구성

### 5.1 게임 화면
```
┌─────────────────────────────────────┐
│  SCORE: 15          BEST: 42        │  ← 상단 UI
├─────────────────────────────────────┤
│                              |      │
│                              |      │
│    ○                         |      │  ← 메인 게임 영역
│   ╱                        ━━┻━━    │
│  ●                                  │  ← 발사 지점
│                                     │
└─────────────────────────────────────┘
```

### 5.2 화면 구성 요소
| 요소 | 위치 | 설명 |
|------|------|------|
| 현재 점수 | 좌상단 | 현재 게임에서 성공한 링 개수 |
| 최고 점수 | 우상단 | 역대 최고 기록 (로컬 저장) |
| 발사 지점 | 좌측 하단 | 링을 던지는 시작점 |
| 바 | 우측 | 링을 걸 목표물 |
| 궤적 가이드 | 조준 시 | 예상 비행 경로 (점선) |

### 5.3 게임 오버 화면
```
┌─────────────────────────────────────┐
│                                     │
│           GAME OVER                 │
│                                     │
│         YOUR SCORE: 15              │
│         BEST: 42                    │
│                                     │
│        [ 다시 하기 ]                │
│                                     │
└─────────────────────────────────────┘
```

## 6. 점수 시스템

| 항목 | 점수 |
|------|------|
| 링 성공 | +1점 |
| 실패 | 게임 오버 |

- 최고 점수는 브라우저 로컬 스토리지에 저장
- 복잡한 콤보나 보너스 시스템 없음 (심플하게 유지)

## 7. 기술 스택

| 항목 | 기술 |
|------|------|
| 언어 | JavaScript (ES6+) |
| 렌더링 | HTML5 Canvas |
| 물리 | 간단한 포물선 운동 (자체 구현) |
| 저장 | LocalStorage |
| 스타일 | CSS3 |

## 8. 개발 우선순위

### Phase 1: 코어 게임플레이
1. 캔버스 설정 및 기본 렌더링
2. 바 그리기
3. 링 그리기
4. 던지기 입력 처리 (마우스 드래그)
5. 포물선 물리 적용
6. 충돌 감지 (링 ↔ 바)

### Phase 2: 게임 로직
7. 성공/실패 판정
8. 점수 시스템
9. 게임 오버 처리
10. 재시작 기능

### Phase 3: 마무리
11. UI 꾸미기
12. 최고 점수 저장
13. 사운드 효과 (선택)
14. 모바일 터치 지원

## 9. 파일 구조 (예정)

```
perfect-ring-throw/
├── index.html          # 메인 HTML
├── css/
│   └── style.css       # 스타일
├── js/
│   ├── game.js         # 게임 메인 로직
│   ├── ring.js         # 링 클래스
│   ├── bar.js          # 바 클래스
│   ├── physics.js      # 물리 계산
│   └── ui.js           # UI 처리
└── assets/
    └── sounds/         # 효과음 (선택)
```

## 10. 확장 가능성 (향후)

현재 버전에서는 구현하지 않지만, 추후 확장 가능한 기능:
- 바의 위치/높이 랜덤 변화
- 움직이는 바
- 다양한 링 스킨
- 리더보드 (온라인)
- 일일 챌린지

---

**문서 버전**: 1.0
**작성일**: 2026-01-16
