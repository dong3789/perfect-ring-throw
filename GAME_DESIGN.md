# 퍼펙트 링 토스 (Perfect Ring Toss) 게임 기획서

## 1. 게임 개요

| 항목 | 내용 |
|------|------|
| 게임명 | Perfect Ring Toss |
| 장르 | 캐주얼 / 아케이드 |
| 플랫폼 | 웹 브라우저 (HTML5 Canvas) |
| 목표 | 링을 바(막대)에 최대한 많이 걸기 |

## 2. 핵심 게임플레이

### 2.1 기본 규칙
- 화면에 수직으로 서있는 **바(막대)**가 있음
- 플레이어는 **링**을 던져서 바에 걸어야 함
- **성공**: 링이 바에 걸림 → 점수 획득 → 다음 링 던지기
- **실패**: 링이 바에 걸리지 않음 → **즉시 게임 오버**
- 난이도 시스템 없음 (단일 난이도)

### 2.2 게임 목표
- 실수 없이 최대한 많은 링을 연속으로 성공시키기
- 최고 기록(하이스코어) 갱신

## 3. 게임 요소

### 3.1 바 (Bar/Pole)
```
     |
     |
     |  ← 바 (링을 걸 목표물)
     |
   ━━┻━━
```
- 화면 우측에 고정 위치
- 수직으로 서있는 막대 형태
- 링이 걸릴 수 있는 히트박스 보유

### 3.2 링 (Ring)
```
   ╭───╮
   │   │  ← 링 (도넛 형태)
   ╰───╯
```
- 화면 좌측에서 던짐
- 포물선 궤적으로 날아감
- 바에 정확히 걸려야 성공

### 3.3 던지기 메커니즘
- **조준**: 마우스/터치로 각도 조절
- **파워**: 드래그 거리 또는 게이지로 힘 조절
- **발사**: 마우스 버튼 릴리즈 / 터치 릴리즈

## 4. 게임 흐름

```
[게임 시작]
     ↓
[링 조준 & 던지기]
     ↓
   성공? ──Yes──→ [점수 +1] → [다음 링 던지기]
     │
    No
     ↓
[게임 오버]
     ↓
[결과 화면: 점수 표시]
     ↓
[재시작 또는 종료]
```

## 5. UI 구성

### 5.1 게임 화면
```
┌─────────────────────────────────────┐
│  SCORE: 15          BEST: 42        │  ← 상단 UI
├─────────────────────────────────────┤
│                              |      │
│                              |      │
│    ○                         |      │  ← 메인 게임 영역
│   ╱                        ━━┻━━    │
│  ●                                  │  ← 발사 지점
│                                     │
└─────────────────────────────────────┘
```

### 5.2 화면 구성 요소
| 요소 | 위치 | 설명 |
|------|------|------|
| 현재 점수 | 좌상단 | 현재 게임에서 성공한 링 개수 |
| 최고 점수 | 우상단 | 역대 최고 기록 (로컬 저장) |
| 발사 지점 | 좌측 하단 | 링을 던지는 시작점 |
| 바 | 우측 | 링을 걸 목표물 |
| 궤적 가이드 | 조준 시 | 예상 비행 경로 (점선) |

### 5.3 게임 오버 화면
```
┌─────────────────────────────────────┐
│                                     │
│           GAME OVER                 │
│                                     │
│         YOUR SCORE: 15              │
│         BEST: 42                    │
│                                     │
│        [ 다시 하기 ]                │
│                                     │
└─────────────────────────────────────┘
```

## 6. 점수 시스템

| 항목 | 점수 |
|------|------|
| 링 성공 | +1점 |
| 실패 | 게임 오버 |

- 최고 점수는 브라우저 로컬 스토리지에 저장
- 복잡한 콤보나 보너스 시스템 없음 (심플하게 유지)

## 7. 멀티플레이어 모드 (온라인 턴제)

### 7.1 개요
**하나의 글로벌 게임**에 모두가 참여하는 온라인 턴제 모드
- 온라인 모드 누르면 바로 대기열 참가
- **인원 제한 없음** - 누구나 언제든 참가 가능
- 순서대로 돌아가며 던지기
- 실시간 통신: **Socket.io** (WebSocket)

### 7.2 게임 흐름
```
[메인 화면]
     ↓
┌─────────────┬─────────────┐
│  솔로 모드   │  온라인 모드  │
└─────────────┴─────────────┘
                    ↓
           [닉네임 입력]
                    ↓
           [대기열 참가]
           (현재 진행 중인 게임 관전)
                    ↓
           [내 차례가 되면]
           실패할 때까지 던지기
                    ↓
           [실패 → 점수 확정]
           (다시 대기열 맨 뒤로)
                    ↓
              ... 무한 반복 ...
```

### 7.3 시스템 구조
```
┌─────────────────────────────────────────────┐
│                  서버                        │
├─────────────────────────────────────────────┤
│  현재 플레이어: 철수 (점수: 8)               │
│                                             │
│  대기열:                                     │
│  ┌─────┬─────┬─────┬─────┬─────┐           │
│  │영희 │민수 │지훈 │새유저│ ... │ ← 무제한   │
│  └─────┴─────┴─────┴─────┴─────┘           │
│                                             │
│  관전자: 28명                                │
└─────────────────────────────────────────────┘
```

### 7.4 메인 게임 화면
```
┌─────────────────────────────────────┐
│  🎯 철수's Turn         SCORE: 8    │
│  👥 12명 대기 중   👀 28명 관전 중   │
├─────────────────────────────────────┤
│                              |      │
│    ○  ← 실시간 중계          |      │
│   ╱                        ━━┻━━    │
│  ●                                  │
├─────────────────────────────────────┤
│  대기열: 영희 → 민수 → 지훈 → 나(4번째) │
└─────────────────────────────────────┘
```

### 7.5 내 차례 UI
```
┌─────────────────────────────────────┐
│  🔥 YOUR TURN!          SCORE: 3    │
│  👥 15명 대기 중   👀 42명 관전 중   │
├─────────────────────────────────────┤
│                              |      │
│                              |      │
│                            ━━┻━━    │
│  ● ← 드래그하여 던지기              │
├─────────────────────────────────────┤
│  🏆 현재 1위: ProGamer (127점)      │
└─────────────────────────────────────┘
```

### 7.6 실패 시 화면
```
┌─────────────────────────────────────┐
│           💀 GAME OVER 💀           │
├─────────────────────────────────────┤
│                                     │
│         YOUR SCORE: 8               │
│                                     │
│    🏆 오늘 순위: #42 / 1,234명       │
│    📈 개인 최고: 15                  │
│                                     │
├─────────────────────────────────────┤
│  [ 다시 대기열 참가 ]  [ 관전만 하기 ] │
└─────────────────────────────────────┘
```

### 7.7 Socket.io 이벤트

| 이벤트 | 방향 | 설명 |
|--------|------|------|
| `join-queue` | Client→Server | 대기열 참가 (닉네임) |
| `queue-update` | Server→All | 대기열 현황 업데이트 |
| `your-turn` | Server→Client | 내 차례 알림 |
| `turn-start` | Server→All | 누구 차례인지 전체 알림 |
| `throw-ring` | Client→Server | 링 던지기 (각도, 힘) |
| `ring-update` | Server→All | 링 위치 실시간 중계 |
| `throw-result` | Server→All | 성공/실패 결과 |
| `player-out` | Server→All | 플레이어 탈락 + 점수 |
| `spectator-count` | Server→All | 관전자 수 업데이트 |
| `leave-queue` | Client→Server | 대기열 이탈 |

## 8. 온라인 기록 저장 (Render 배포)

### 8.1 Render 무료 플랜 특성
| 특성 | 설명 | 대응 방안 |
|------|------|-----------|
| 슬립 모드 | 15분 비활성 시 서버 슬립 | 첫 요청 시 로딩 표시 |
| 콜드 스타트 | 슬립 후 재시작 ~30초 | 로컬 캐시 우선 표시 |
| PostgreSQL | 무료 DB 제공 | 리더보드 저장에 사용 |
| 파일 시스템 | 재시작 시 초기화 | DB만 사용 |

### 8.2 리더보드 시스템
```
┌─────────────────────────────────────┐
│         🏆 LEADERBOARD 🏆           │
├─────────────────────────────────────┤
│   #    이름           점수   날짜   │
├─────────────────────────────────────┤
│   1    ProGamer       127   01/15  │
│   2    철수            89   01/14  │
│   3    RingMaster      76   01/13  │
│   ...                               │
│   42   나 (현재)        15   01/16  │
├─────────────────────────────────────┤
│  [ 닫기 ]                           │
└─────────────────────────────────────┘
```

### 8.3 기록 저장 방식
1. **게임 오버 시** → 점수 + 닉네임 입력 팝업
2. **서버 전송** → POST /api/scores
3. **리더보드 조회** → GET /api/scores (상위 100명)
4. **오프라인 대비** → LocalStorage 백업

### 8.4 API 설계

| 엔드포인트 | 메서드 | 설명 |
|------------|--------|------|
| `/api/scores` | GET | 리더보드 조회 (상위 100) |
| `/api/scores` | POST | 새 점수 등록 |
| `/api/scores/:name` | GET | 특정 유저 기록 조회 |

**POST /api/scores 요청**
```json
{
  "name": "철수",
  "score": 15
}
```

**GET /api/scores 응답**
```json
{
  "leaderboard": [
    { "rank": 1, "name": "ProGamer", "score": 127, "date": "2026-01-15" },
    { "rank": 2, "name": "철수", "score": 89, "date": "2026-01-14" }
  ]
}
```

## 9. 기술 스택

### 9.1 프론트엔드
| 항목 | 기술 |
|------|------|
| 언어 | JavaScript (ES6+) |
| 렌더링 | HTML5 Canvas |
| 물리 | 간단한 포물선 운동 (자체 구현) |
| 로컬 저장 | LocalStorage |
| 스타일 | CSS3 |

### 9.2 백엔드 (Render 배포)
| 항목 | 기술 |
|------|------|
| 런타임 | Node.js |
| 프레임워크 | Express.js |
| 실시간 통신 | Socket.io |
| 데이터베이스 | PostgreSQL (Render 무료) |
| ORM | 없음 (pg 직접 사용) |

## 10. 개발 우선순위

### Phase 1: 코어 게임플레이
1. 캔버스 설정 및 기본 렌더링
2. 바 그리기
3. 링 그리기
4. 던지기 입력 처리 (마우스 드래그)
5. 포물선 물리 적용
6. 충돌 감지 (링 ↔ 바)

### Phase 2: 솔로 게임 로직
7. 성공/실패 판정
8. 점수 시스템
9. 게임 오버 처리
10. 재시작 기능

### Phase 3: 서버 구축
11. Express + Socket.io 서버 설정
12. PostgreSQL 연동
13. 리더보드 API (점수 저장/조회)

### Phase 4: 온라인 멀티플레이어
14. 대기열 시스템 구현
15. 턴 관리 로직
16. 실시간 게임 상태 동기화 (링 위치 중계)
17. 관전 모드
18. 프론트엔드 Socket.io 연동

### Phase 5: 마무리
19. 모드 선택 화면 (솔로/온라인)
20. UI 꾸미기
21. 모바일 터치 지원
22. Render 배포

## 11. 파일 구조 (예정)

```
perfect-ring-throw/
├── public/                     # 정적 파일 (프론트엔드)
│   ├── index.html              # 메인 HTML
│   ├── css/
│   │   └── style.css           # 스타일
│   └── js/
│       ├── game.js             # 게임 메인 로직
│       ├── ring.js             # 링 클래스
│       ├── bar.js              # 바 클래스
│       ├── physics.js          # 물리 계산
│       ├── ui.js               # UI 처리
│       └── socket-client.js    # Socket.io 클라이언트
├── server/
│   ├── index.js                # Express + Socket.io 서버
│   ├── db.js                   # PostgreSQL 연결
│   ├── gameManager.js          # 대기열 & 턴 관리
│   └── routes/
│       └── scores.js           # 리더보드 API
├── package.json                # 의존성 관리
├── render.yaml                 # Render 배포 설정
└── README.md
```

## 12. Render 배포 설정

### 12.1 render.yaml
```yaml
services:
  - type: web
    name: perfect-ring-toss
    env: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ring-toss-db
          property: connectionString

databases:
  - name: ring-toss-db
    plan: free
```

### 12.2 데이터베이스 스키마
```sql
CREATE TABLE scores (
    id SERIAL PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    score INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_scores_score ON scores(score DESC);
```

## 13. 확장 가능성 (향후)

현재 버전에서는 구현하지 않지만, 추후 확장 가능한 기능:
- 바의 위치/높이 랜덤 변화
- 움직이는 바
- 다양한 링 스킨
- 일일 챌린지
- 친구 초대 (방 시스템)

---

**문서 버전**: 3.0
**작성일**: 2026-01-16
**변경사항**: 온라인 턴제 멀티플레이어 (글로벌 대기열 방식), Socket.io 실시간 통신
